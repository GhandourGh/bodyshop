# =============================================================================
# Runtime — required to run main.py (the FastAPI AI microservice)
# =============================================================================
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
python-dotenv==1.0.1
slowapi==0.1.9
apscheduler==3.10.4

# Database
psycopg2-binary==2.9.9

# ML inference
joblib==1.4.2
xgboost==2.1.1
scikit-learn==1.5.2
pandas==2.2.3
numpy==2.4.4

# Computer vision (YOLOv8 damage detection)
ultralytics==8.4.45
opencv-python-headless==4.13.0.92
Pillow==10.4.0

# Time-series forecasting (inventory)
prophet==1.3.0

# LLM / NLP
groq==1.2.0
transformers==5.7.0
accelerate>=1.1.0

# =============================================================================
# Training scripts in scripts/ — install only if you plan to retrain models.
# `pip install -r requirements.txt` already pulls these in for simplicity.
# =============================================================================
torch>=2.4
datasets>=2.20
huggingface_hub>=0.24
